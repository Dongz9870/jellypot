<UserControl x:Class="JellyPot.App.Views.LoginView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <Grid Background="{StaticResource BackgroundBrush}">
        <Ellipse Width="700" Height="700" HorizontalAlignment="Left" VerticalAlignment="Top" Margin="-330,-390,0,0" Opacity="0.16">
            <Ellipse.Fill><RadialGradientBrush><GradientStop Color="#FF4D8D" Offset="0" /><GradientStop Color="#0B0D12" Offset="1" /></RadialGradientBrush></Ellipse.Fill>
        </Ellipse>
        <Grid MaxWidth="1120" Margin="54">
            <Grid.ColumnDefinitions><ColumnDefinition Width="1.1*" /><ColumnDefinition Width="0.9*" /></Grid.ColumnDefinitions>

            <StackPanel VerticalAlignment="Center" Margin="20,0,80,0">
                <StackPanel Orientation="Horizontal" Margin="0,0,0,54">
                    <Border Width="48" Height="48" CornerRadius="15" Background="{StaticResource AccentBrush}">
                        <TextBlock Text="J" FontWeight="Black" FontSize="29" HorizontalAlignment="Center" VerticalAlignment="Center" />
                    </Border>
                    <StackPanel Margin="14,0,0,0" VerticalAlignment="Center">
                        <TextBlock Text="JellyPot" FontSize="24" FontWeight="Bold" />
                        <TextBlock Text="J E L L Y F I N  ×  P O T P L A Y E R" Foreground="{StaticResource MutedTextBrush}" FontSize="9" />
                    </StackPanel>
                </StackPanel>

                <TextBlock Text="你的电影，&#x0a;按你的方式播放。" FontSize="48" FontWeight="Bold" LineHeight="59" />
                <TextBlock Text="Jellyfin 管理资料与海报，PotPlayer 直接读取本地硬盘或 NAS 原文件。没有转码，也无需改变你熟悉的播放配置。"
                           Foreground="{StaticResource MutedTextBrush}" FontSize="15" LineHeight="25" TextWrapping="Wrap" Margin="0,24,0,34" MaxWidth="540" HorizontalAlignment="Left" />

                <UniformGrid Columns="3">
                    <StackPanel Margin="0,0,18,0"><TextBlock Text="01" Foreground="{StaticResource AccentBrush}" FontWeight="Bold" /><TextBlock Text="连接 Jellyfin" Margin="0,7,0,0" /><TextBlock Text="海报与资料" Foreground="{StaticResource MutedTextBrush}" FontSize="11" /></StackPanel>
                    <StackPanel Margin="0,0,18,0"><TextBlock Text="02" Foreground="{StaticResource PurpleBrush}" FontWeight="Bold" /><TextBlock Text="转换路径" Margin="0,7,0,0" /><TextBlock Text="本地 / UNC" Foreground="{StaticResource MutedTextBrush}" FontSize="11" /></StackPanel>
                    <StackPanel><TextBlock Text="03" Foreground="{StaticResource SuccessBrush}" FontWeight="Bold" /><TextBlock Text="PotPlayer" Margin="0,7,0,0" /><TextBlock Text="原文件直读" Foreground="{StaticResource MutedTextBrush}" FontSize="11" /></StackPanel>
                </UniformGrid>
            </StackPanel>

            <Border Grid.Column="1" Style="{StaticResource SectionCard}" Padding="34" VerticalAlignment="Center">
                <StackPanel>
                    <TextBlock Text="连接媒体服务器" FontSize="24" FontWeight="SemiBold" />
                    <TextBlock Text="登录信息只发送给你的 Jellyfin 服务器" Foreground="{StaticResource MutedTextBrush}" Margin="0,7,0,28" />

                    <TextBlock Text="服务器地址" Foreground="{StaticResource MutedTextBrush}" FontSize="12" Margin="0,0,0,7" />
                    <Grid>
                        <TextBox Text="{Binding ServerUrl, UpdateSourceTrigger=PropertyChanged}" />
                        <Button Content="测试" Command="{Binding TestConnectionCommand}" Style="{StaticResource GhostButton}" HorizontalAlignment="Right" Padding="12,4" MinHeight="30" Margin="0,4,5,4" Foreground="{StaticResource AccentBrush}" />
                    </Grid>

                    <TextBlock Text="用户名" Foreground="{StaticResource MutedTextBrush}" FontSize="12" Margin="0,18,0,7" />
                    <TextBox Text="{Binding Username, UpdateSourceTrigger=PropertyChanged}" />
                    <TextBlock Text="密码" Foreground="{StaticResource MutedTextBrush}" FontSize="12" Margin="0,18,0,7" />
                    <PasswordBox PasswordChanged="PasswordBox_OnPasswordChanged" />

                    <Border Background="#11151D" CornerRadius="7" Padding="11,8" Margin="0,18,0,0">
                        <TextBlock Text="{Binding StatusMessage}" TextWrapping="Wrap" FontSize="12">
                            <TextBlock.Style>
                                <Style TargetType="TextBlock"><Setter Property="Foreground" Value="{StaticResource SuccessBrush}" /><Style.Triggers><DataTrigger Binding="{Binding StatusIsError}" Value="True"><Setter Property="Foreground" Value="{StaticResource ErrorBrush}" /></DataTrigger></Style.Triggers></Style>
                            </TextBlock.Style>
                        </TextBlock>
                    </Border>

                    <Button Content="登录并进入电影库  →" Command="{Binding LoginCommand}" Style="{StaticResource PrimaryButton}" Margin="0,22,0,10" />
                    <Button Content="先看看界面设计" Command="{Binding DemoCommand}" Style="{StaticResource GhostButton}" />
                    <TextBlock Text="密码不会保存；访问令牌由 Windows 凭据管理器保护。" Foreground="{StaticResource MutedTextBrush}" TextAlignment="Center" FontSize="10" Margin="0,16,0,0" />
                </StackPanel>
            </Border>
        </Grid>
    </Grid>
</UserControl>
