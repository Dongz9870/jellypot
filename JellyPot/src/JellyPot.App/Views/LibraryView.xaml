<UserControl x:Class="JellyPot.App.Views.LibraryView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <Grid Margin="30,24,30,20">
        <Grid.RowDefinitions><RowDefinition Height="Auto" /><RowDefinition Height="Auto" /><RowDefinition Height="*" /><RowDefinition Height="Auto" /></Grid.RowDefinitions>

        <Grid>
            <StackPanel>
                <TextBlock Text="{Binding SectionTitle}" FontSize="30" FontWeight="Bold" />
                <TextBlock Text="{Binding ResultText}" Foreground="{StaticResource MutedTextBrush}" Margin="0,6,0,0" />
            </StackPanel>
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" VerticalAlignment="Center">
                <ComboBox Width="170" ItemsSource="{Binding Libraries}" DisplayMemberPath="Name" SelectedItem="{Binding SelectedLibrary}" Margin="0,0,10,0" />
                <Grid Width="270">
                    <TextBox Text="{Binding SearchText, UpdateSourceTrigger=PropertyChanged}" Padding="36,8,12,8" />
                    <TextBlock Text="⌕" Foreground="{StaticResource MutedTextBrush}" FontSize="19" Margin="12,0,0,0" VerticalAlignment="Center" HorizontalAlignment="Left" />
                    <Button Command="{Binding SearchCommand}" Content="搜索" Style="{StaticResource GhostButton}" HorizontalAlignment="Right" Padding="10,4" MinHeight="30" Margin="0,4,5,4" />
                </Grid>
            </StackPanel>
        </Grid>

        <Grid Grid.Row="1" Margin="0,24,0,18">
            <StackPanel Orientation="Horizontal">
                <ComboBox Width="128" ItemsSource="{Binding Filters}" SelectedItem="{Binding SelectedFilter}" Margin="0,0,10,0" />
                <ComboBox Width="148" ItemsSource="{Binding SortOptions}" SelectedItem="{Binding SelectedSort}" />
            </StackPanel>
            <Button HorizontalAlignment="Right" Content="↻  刷新" Command="{Binding RefreshCommand}" Style="{StaticResource GhostButton}" />
        </Grid>

        <Grid Grid.Row="2">
            <ListBox ItemsSource="{Binding VisibleMovies}" Background="Transparent" BorderThickness="0" ScrollViewer.HorizontalScrollBarVisibility="Disabled" ScrollViewer.VerticalScrollBarVisibility="Auto">
                <ListBox.ItemsPanel>
                    <ItemsPanelTemplate><WrapPanel IsItemsHost="True" Orientation="Horizontal" /></ItemsPanelTemplate>
                </ListBox.ItemsPanel>
                <ListBox.ItemContainerStyle>
                    <Style TargetType="ListBoxItem">
                        <Setter Property="Background" Value="Transparent" />
                        <Setter Property="BorderThickness" Value="0" />
                        <Setter Property="Padding" Value="0" />
                        <Setter Property="Margin" Value="0" />
                        <Setter Property="FocusVisualStyle" Value="{x:Null}" />
                    </Style>
                </ListBox.ItemContainerStyle>
                <ListBox.ItemTemplate>
                    <DataTemplate>
                        <Button Command="{Binding DataContext.OpenMovieCommand, RelativeSource={RelativeSource AncestorType=ListBox}}" CommandParameter="{Binding}"
                                Background="Transparent" BorderThickness="0" Padding="0" Margin="0,0,18,24" Cursor="Hand" FocusVisualStyle="{x:Null}">
                            <StackPanel Width="174">
                                <Border Height="260" CornerRadius="10" Background="{StaticResource SurfaceRaisedBrush}" ClipToBounds="True">
                                    <Grid>
                                        <Border>
                                            <Border.Background>
                                                <LinearGradientBrush StartPoint="0,0" EndPoint="1,1">
                                                    <GradientStop Color="#252B38" Offset="0" /><GradientStop Color="#151923" Offset="1" />
                                                </LinearGradientBrush>
                                            </Border.Background>
                                            <TextBlock Text="{Binding Initial}" Foreground="#3A4150" FontSize="72" FontWeight="Black" HorizontalAlignment="Center" VerticalAlignment="Center" />
                                        </Border>
                                        <Image Source="{Binding PosterUrl}" Stretch="UniformToFill" />
                                        <StackPanel Orientation="Horizontal" HorizontalAlignment="Left" VerticalAlignment="Top" Margin="9">
                                            <Border Background="#D90B0D12" CornerRadius="5" Padding="7,3" ToolTip="{Binding ResolutionDetailText}">
                                                <TextBlock Text="{Binding ResolutionText}" FontSize="10" FontWeight="Bold" />
                                            </Border>
                                            <Border CornerRadius="5" Padding="7,3" Margin="5,0,0,0" ToolTip="{Binding DynamicRangeDetailText}"
                                                    Visibility="{Binding HasDynamicRange, Converter={StaticResource BoolToVisibility}}">
                                                <Border.Style>
                                                    <Style TargetType="Border">
                                                        <Setter Property="Background" Value="#D93A414F" />
                                                        <Style.Triggers>
                                                            <DataTrigger Binding="{Binding DynamicRangeText}" Value="HDR"><Setter Property="Background" Value="#D9823B64" /></DataTrigger>
                                                        </Style.Triggers>
                                                    </Style>
                                                </Border.Style>
                                                <TextBlock Text="{Binding DynamicRangeText}" FontSize="10" FontWeight="Bold" />
                                            </Border>
                                            <Border Background="#D9295D8F" CornerRadius="5" Padding="7,3" Margin="5,0,0,0" ToolTip="{Binding BluRayDetailText}"
                                                    Visibility="{Binding HasBluRay, Converter={StaticResource BoolToVisibility}}">
                                                <TextBlock Text="蓝光" Foreground="#D9ECFF" FontSize="10" FontWeight="Bold" />
                                            </Border>
                                        </StackPanel>
                                        <Border HorizontalAlignment="Right" VerticalAlignment="Top" Background="#D90B0D12" CornerRadius="13" Width="26" Height="26" Margin="9"
                                                Visibility="{Binding UserData.IsFavorite, Converter={StaticResource BoolToVisibility}}">
                                            <TextBlock Text="♥" Foreground="{StaticResource AccentBrush}" HorizontalAlignment="Center" VerticalAlignment="Center" />
                                        </Border>
                                        <Border VerticalAlignment="Bottom" Height="4" Background="#66000000">
                                            <ProgressBar Value="{Binding ProgressPercent, Mode=OneWay}" Maximum="100" Foreground="{StaticResource AccentBrush}" Background="Transparent" BorderThickness="0" />
                                        </Border>
                                    </Grid>
                                </Border>
                                <TextBlock Text="{Binding Name}" FontSize="14" FontWeight="SemiBold" TextTrimming="CharacterEllipsis" Margin="2,10,2,0" />
                                <Grid Margin="2,5,2,0">
                                    <TextBlock Text="{Binding YearText}" Foreground="{StaticResource MutedTextBrush}" FontSize="11" />
                                    <TextBlock Text="{Binding RatingText, StringFormat=★ {0}}" Foreground="#F6C85F" FontSize="11" HorizontalAlignment="Right" />
                                </Grid>
                            </StackPanel>
                        </Button>
                    </DataTemplate>
                </ListBox.ItemTemplate>
            </ListBox>

            <StackPanel Visibility="{Binding IsEmpty, Converter={StaticResource BoolToVisibility}}" HorizontalAlignment="Center" VerticalAlignment="Center">
                <Border Width="64" Height="64" CornerRadius="20" Background="{StaticResource SurfaceRaisedBrush}" HorizontalAlignment="Center">
                    <TextBlock Text="▦" FontSize="28" Foreground="{StaticResource MutedTextBrush}" HorizontalAlignment="Center" VerticalAlignment="Center" />
                </Border>
                <TextBlock Text="此分类还没有内容" FontSize="18" FontWeight="SemiBold" HorizontalAlignment="Center" Margin="0,18,0,0" />
                <TextBlock Text="检查片源库选择，或尝试清空搜索与筛选条件。" Foreground="{StaticResource MutedTextBrush}" Margin="0,7,0,0" />
            </StackPanel>

            <Border Visibility="{Binding IsBusy, Converter={StaticResource BoolToVisibility}}" Background="#A60B0D12">
                <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center">
                    <ProgressBar IsIndeterminate="True" Width="150" Height="3" Foreground="{StaticResource AccentBrush}" />
                    <TextBlock Text="正在从 Jellyfin 加载电影…" Foreground="{StaticResource MutedTextBrush}" Margin="0,14,0,0" />
                </StackPanel>
            </Border>
        </Grid>

        <Grid Grid.Row="3" Margin="0,12,0,0">
            <TextBlock Text="{Binding ErrorMessage}" Foreground="{StaticResource ErrorBrush}" VerticalAlignment="Center" TextTrimming="CharacterEllipsis" />
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                <Button Content="←" Command="{Binding PreviousPageCommand}" Style="{StaticResource SecondaryButton}" MinWidth="44" Padding="10,7" />
                <TextBlock Text="{Binding PageText}" VerticalAlignment="Center" Margin="14,0" Foreground="{StaticResource MutedTextBrush}" />
                <Button Content="→" Command="{Binding NextPageCommand}" Style="{StaticResource SecondaryButton}" MinWidth="44" Padding="10,7" />
            </StackPanel>
        </Grid>
    </Grid>
</UserControl>
